#!/usr/bin/env ruby

$LOAD_PATH.unshift File.expand_path("../lib", __FILE__)
$LOAD_PATH.unshift File.expand_path("../ext", __FILE__)

require 'rubygems'
require 'gui/GLUTViewer2'

controller = GLUTViewer2.new
controller.doArgs(ARGV)
